<launch>
  <!-- if running sim across different machines, see rep 124 for detaails
       on env-loader attribute -->
  <machine name="c1" address="localhost" default="true" />   
  <machine name="c2" address="localhost" />   
</launch>
